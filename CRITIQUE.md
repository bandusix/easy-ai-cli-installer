# AI CLI Installer - 项目批判与优化方案

## 🔴 严重问题

### 1. **GUI 未实际更新到仓库**
**问题**: 修改的 `gui_installer.py` 在 `C:\Users\zhichao5.chen\Downloads\` 下,但仓库里的是 `easy-ai-cli-installer/gui_installer.py`,两个文件不一致。
**影响**: 用户下载的 installer 仍然是旧的 Fluent/macOS 风格,不是黑客风格。
**修复**: 复制修改后的 GUI 到仓库,重新打包测试。

### 2. **PyInstaller 配置未指定图标**
**问题**: `build.spec` 没有引用新设计的 `icon-*.png`,打包出来的 .exe/.app 用的是默认图标。
**影响**: 用户看到的是 Python 默认图标,不是我们的终端图标。
**修复**: 转换 PNG → ICO/ICNS,更新 `build.spec` 的 `icon=` 参数。

### 3. **官网版本号更新 workflow 未测试**
**问题**: `update-website.yml` 的 sed 正则未验证,可能匹配失败。
**影响**: 版本号可能不会自动更新。
**修复**: 手动触发 workflow 验证,检查 sed 输出。

### 4. **README 截图路径未验证**
**问题**: 新截图已替换,但 README 里引用的路径可能还是旧的。
**影响**: GitHub 上显示的截图可能 404 或显示旧图。
**修复**: 检查 README 中所有图片引用路径。

---

## 🟡 中等问题

### 5. **GUI 字体在部分系统可能不存在**
**问题**: `Courier New` (Windows) 和 `Monaco` (macOS) 不一定在所有系统都有,尤其是 Wine 或虚拟机环境。
**影响**: 界面回退到默认字体,失去等宽美感。
**修复**: 添加 fallback 字体列表,或打包 monospace 字体。

### 6. **黑色背景在亮屏环境下眼睛疲劳**
**问题**: 纯黑背景 (#000) 在白天/室外亮屏时对比度过高,眼睛刺激。
**影响**: 部分用户体验差。
**修复**: 可选主题切换(黑客模式 vs. 高对比度模式)或改用 #0A0A0A(接近黑但不刺眼)。

### 7. **官网在移动端语言切换器重叠**
**问题**: 6 个语言按钮固定在右上角,小屏幕可能超出边界或遮挡内容。
**影响**: 移动端用户无法切换语言。
**修复**: 添加 `@media (max-width: 768px)` 响应式布局,改为下拉菜单或底部固定。

### 8. **CI workflow 没有失败通知**
**问题**: 如果 build 失败,只能手动去 Actions 页面看,没有邮件/Slack 通知。
**影响**: 构建失败可能几天都不知道。
**修复**: 添加 GitHub Actions 邮件通知或 webhook。

---

## 🟢 优化建议

### 9. **官网缺少使用演示视频/GIF**
**问题**: 只有静态截图,用户不知道安装流程有多简单。
**影响**: 转化率低。
**优化**: 录制 10 秒 GIF:下载 → 双击 → 勾选 → 安装完成。

### 10. **README 没有对比表格(vs. 手动安装)**
**问题**: 没有突出"为什么用 installer",用户不知道省了多少麻烦。
**优化**: 添加对比表:
| 方式 | 步骤数 | 需要技能 | 时间 |
|------|--------|----------|------|
| 手动 | 15+ | npm/PATH/环境变量 | 30 分钟 |
| Installer | 3 | 双击 | 2 分钟 |

### 11. **官网缺少 FAQ**
**问题**: 常见问题(如"支持 Intel Mac 吗?""需要 sudo 吗?")没有解答。
**影响**: 用户犹豫不敢下载。
**优化**: 添加 FAQ 折叠区块。

### 12. **GUI 没有进度百分比显示**
**问题**: 安装时只显示 "Installing...",不知道进度。
**影响**: 用户以为卡死了,强制关闭。
**优化**: 添加 `1/5 Installing Claude Code...` 进度提示。

### 13. **没有卸载功能**
**问题**: 用户想卸载某个 CLI,没有对应的 uninstaller。
**影响**: 需要手动删 bin/ 目录下的文件,不友好。
**优化**: 添加 `--uninstall` 模式,或提供卸载脚本。

### 14. **官网 i18n 加载是 fetch,可能被广告拦截器阻止**
**问题**: `fetch('i18n.json')` 在某些浏览器扩展下可能被拦截。
**影响**: 语言切换失效,界面显示 undefined。
**优化**: 将 i18n 内联到 HTML `<script>` 里,或用 `<script src="i18n.js">`。

### 15. **版本号自动更新依赖 grep 正则,容易失败**
**问题**: `grep -oP 'claude-code.*latest=\K[v0-9.]+'` 如果 fetch_latest.py 输出格式变了,就匹配不到。
**影响**: 版本号停止更新。
**优化**: 改用 Python 脚本直接调用 fetch_latest API,输出 JSON,用 jq 解析。

---

## 🔵 长期改进

### 16. **支持 Linux 平台**
**当前**: 只支持 Windows + macOS。
**改进**: 添加 Linux .AppImage 或 .deb 打包。

### 17. **支持更多 AI CLI**
**当前**: 5 款 CLI。
**改进**: 加入 Cursor CLI, Tabnine CLI, Replit Agent 等。

### 18. **GUI 支持插件系统**
**当前**: 工具列表硬编码。
**改进**: 用 JSON 配置文件定义工具,用户可自定义添加。

### 19. **官网支持 PWA (Progressive Web App)**
**当前**: 普通静态网页。
**改进**: 添加 manifest.json,支持"添加到主屏幕",离线访问。

---

## 📋 修复优先级

### P0 (立即修复,否则项目不可用)
1. ✅ GUI 未实际更新到仓库
2. ✅ PyInstaller 图标未配置
3. ✅ README 截图路径验证

### P1 (本轮必须修复)
4. ✅ 官网移动端语言切换器布局
5. ✅ 官网 i18n fetch 改为内联
6. ✅ GUI 字体 fallback
7. ✅ GUI 进度显示优化

### P2 (下一版本修复)
8. 版本号更新逻辑改用 Python + JSON
9. CI 失败通知
10. 添加卸载功能
11. 录制演示 GIF
12. README 对比表格

### P3 (长期规划)
13. 主题切换(黑客/高对比度)
14. Linux 支持
15. 插件系统
16. PWA 支持

---

## 🎯 本轮优化目标

修复 P0 + P1 所有问题,确保:
- GUI 黑客风格正确打包到 installer
- 图标在 Windows/macOS 显示正确
- 官网在移动端完美显示
- 安装进度清晰可见
- 所有 README 图片正常显示
