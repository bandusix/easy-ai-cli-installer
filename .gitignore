# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# PyInstaller
# (build.spec is checked in — it's the canonical entry point. Other
# ad-hoc .spec files generated by `pyinstaller foo.py` are not.)
*.spec
!build.spec

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local artifacts
*.log
*.tmp
payload/    # bundled binaries are produced by the GitHub Actions CI workflow
